Map([<Rule '/static/<filename>' (HEAD, GET, OPTIONS) -> static>,
 <Rule '/' (HEAD, GET, OPTIONS) -> index>,
 <Rule '/api/ping' (HEAD, GET, OPTIONS) -> ping>,
 <Rule '/debug-routes' (HEAD, GET, OPTIONS) -> debug_routes>,
 <Rule '/<subpath>' (HEAD, GET, OPTIONS) -> catch_all>])